<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>

    <!-- Used only by IncomingJobActivity, a trampoline that wakes/unlocks the
         screen and immediately forwards to MainActivity. It never actually
         draws UI, so it must be fully transparent — otherwise the default
         AppCompat theme paints an opaque frame (and runs a fade transition)
         that reads as a flash between the notification and the real app. -->
    <style name="Theme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
